steps:
  - label: ":shell: Shellcheck"
    plugins:
      - shellcheck#v1.3.0:
          files:
            - hooks/**

  - label: ":shell: Lint"
    plugins:
      - plugin-linter#v3.2.0:
          id: git-clean

  - label: ":shell: Tests"
    plugins:
      - plugin-tester#v1.1.0: ~
